/**
 * Problem 73: Three Sum Problem
 * Topic: Array
 *
 * Description:
 * You are given an array of integers `nums`. Your task is to find all 
 * unique triplets in the array such that the sum of the three numbers is zero.
 *
 * Conditions:
 * - The result should not contain duplicate triplets.
 * - The order of numbers inside a triplet does not matter 
 *   (e.g., [-1, 0, 1] is the same as [0, -1, 1]).
 * - The solution set can be returned in any order.
 *
 * Input:
 * - An array of integers nums (length n, where n >= 3).
 *
 * Output:
 * - An array of arrays, where each inner array is a triplet [a, b, c] 
 *   such that a + b + c = 0.
 *
 * Examples:
 *
 * Example 1:
 *   Input: [-1, 0, 1, 2, -1, -4]
 *   Output: [[-1, -1, 2], [-1, 0, 1]]
 *
 * Example 2:
 *   Input: [0, 1, 1]
 *   Output: []
 *
 * Example 3:
 *   Input: [0, 0, 0, 0]
 *   Output: [[0, 0, 0]]
 *
 * Instructions:
 * - Implement the solution inside the function below.
 * - Do not modify the function name.
 */


function solution(input) {
  // Your code here
  return input;
}

module.exports = solution;
