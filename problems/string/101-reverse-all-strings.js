/**
 * Problem X: Reverse Each Word in a Sentence
 * Topic: String
 *
 * Description:
 * Write a JavaScript function that takes a sentence as input and reverses each word
 * individually while keeping the word order in the sentence the same.
 * 
 * Rules:
 * - Words are separated by spaces.
 * - Keep the spaces between words intact (including multiple spaces).
 * - Maintain the case of each character.
 * 
 * Example:
 *   Input: "Hello World"
 *   Output: "olleH dlroW"
 * 
 *   Input: "JavaScript is fun"
 *   Output: "tpircSavaJ si nuf"
 * 
 *   Input: "  Keep  spaces  "
 *   Output: "  peeK  secaps  "
 * 
 * Instructions:
 * - Implement the solution inside the function.
 * - Do not modify the function name.
 * - Return the transformed sentence.
 */

function solution(input) {
  // Implement your code here
  return input
}


console.log(solution("JavaScript is fun"));

// module.exports = solution;
