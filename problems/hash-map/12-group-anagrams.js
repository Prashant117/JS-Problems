/**
 * Problem 12: Group Anagrams
 * Topic: Hash Map
 *
 * Description:
 * Given an array of strings `strs`, group the anagrams together.
 *
 * An anagram is a word formed by rearranging the letters of another word, 
 * using all the original letters exactly once.
 *
 * Input:
 * - An array of strings `strs` (1 <= strs.length <= 10^4, 
 *   0 <= strs[i].length <= 100, strs[i] consists of lowercase English letters).
 *
 * Output:
 * - A 2D array (array of arrays), where each inner array contains a group of anagrams.
 *
 * Examples:
 *
 * Example 1:
 *   Input: ["eat", "tea", "tan", "ate", "nat", "bat"]
 *   Output: [["eat", "tea", "ate"], ["tan", "nat"], ["bat"]]
 *
 * Example 2:
 *   Input: [""]
 *   Output: [[""]]
 *
 * Example 3:
 *   Input: ["a"]
 *   Output: [["a"]]
 *
 * Instructions:
 * - Implement the solution inside the function below.
 * - Do not modify the function name.
 */


function solution(input) {
  // Your code here
  return input;
}

module.exports = solution;
