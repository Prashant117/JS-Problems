const solution = require('../../problems/sliding-window/65-minimum-window-substring');

test('Sample test for Minimum Window Substring', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
