const solution = require('../../problems/search/60-search-in-rotated-sorted-array');

test('Sample test for Search in Rotated Sorted Array', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
