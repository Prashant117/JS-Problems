const solution = require('../../problems/math/96-generate-random-hex-color');

test('Sample test for Generate Random Hex Color', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
