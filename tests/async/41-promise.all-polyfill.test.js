const solution = require('../../problems/async/41-promise.all-polyfill');

test('Sample test for Promise.all Polyfill', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
