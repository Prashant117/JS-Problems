const solution = require('../../problems/string/14-first-non-repeating-character');

test('Sample test for First Non-Repeating Character', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
