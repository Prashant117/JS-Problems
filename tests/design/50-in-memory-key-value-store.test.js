const solution = require('../../problems/design/50-in-memory-key-value-store');

test('Sample test for In-memory Key-Value Store', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
