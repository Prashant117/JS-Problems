const solution = require('../../problems/array/51-find-intersection-of-two-arrays');

test('Sample test for Find Intersection of Two Arrays', () => {
  expect(solution('test')).toBe('test'); // Replace with actual tests
});
